---
title: Installation & setup
weight: 4
---

You can install the package via composer:

```bash
composer require spatie/php-attribute-reader
```

That's it. No config files, no service providers — just start using the `Attributes` class.
